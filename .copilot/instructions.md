Do not run commands in the same terminal where the server was started.
Reuse existing idle terminals instead of creating new ones.
Before running a command, ensure the working directory is correct (e.g., the folder containing package.json).
